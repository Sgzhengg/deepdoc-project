# ========== Core Framework ==========
fastapi==0.115.0
uvicorn[standard]==0.30.0
python-multipart==0.0.21
python-dotenv==1.0.0

# ========== 文档处理 - PaddleOCR方案 ==========
# PDF处理
pypdf>=6.0.0                # PDF文本提取
pdf2image>=1.16.0            # PDF转图片（OCR需要）
pymupdf>=1.23.0              # MuPDF（更好的PDF处理）

# PaddleOCR - 表格识别核心
paddleocr>=2.7.0             # PaddleOCR主包
paddlepaddle>=2.5.0          # 飞桨框架（GPU版本如果需要）
# 如果没有GPU，安装CPU版本：
# pip install paddlepaddle -i https://mirror.baidu.com/pypi/simple

# Office文档
python-docx==1.1.0            # Word
openpyxl==3.1.5               # Excel
pandas==2.2.2                # 数据处理

# ========== 向量搜索 ==========
sentence-transformers>=2.7.0
qdrant-client==1.12.0
transformers>=4.34.0

# ========== 文本处理 ==========
jieba==0.42.1                 # 中文分词
rank-bm25==0.2.2              # BM25算法

# ========== 数据验证 ==========
pydantic==2.9.0
pydantic-settings==2.6.0

# ========== 数值计算 ==========
numpy==1.24.0                # 兼容性考虑

# ========== HTTP & Async ==========
requests==2.32.0
aiohttp==3.10.0

# ========== 图像处理 ==========
Pillow==11.3.0
opencv-python-headless>=4.8.0  # 无头版本（服务器推荐）
